---
title: "Changelog"
description: "What's new in each Dax release."
icon: "clock-rotate-left"
---

Stay up to date with what's shipping in Dax.

## 2.39.0

<sub>August 2026</sub>

**Shepherd knows where you are**

Forty-six changes, nearly all in Shepherd. The theme is knowing where you are: panes name themselves after the work in them, colour tells folders apart, and a pane can be picked up and put down somewhere else.

**Every pane says what it's doing**
- **A pane is named after the work in it** — in a few words, read by Apple's on-device model. Nothing leaves your Mac. Panes you've named yourself keep your name. Without Apple Intelligence, panes keep the titles they always had.
- A shimmer shows where the rest of the name is arriving.
- A tab is named for the folder it's working in, so two checkouts of the same repo stop looking like the same place.

**Colour tells folders apart**
- Every tab in a folder shares that folder's colour — the tree, the collapsed strip, the tab bar, the flock and the Agent Monitor all follow at once.
- Settings → Appearance → Colour chooses the scheme: *Each folder* (default) or *Each tab*.

**Getting around**
- **<kbd>⌘</kbd><kbd>J</kbd> leads with where you've lately been.** Bouncing between two agents is one keystroke each way.
- **<kbd>⌃</kbd><kbd>Tab</kbd> steps through agents, not past them.** A folder with four agents across two tabs now visits all of them.
- **<kbd>⌘</kbd><kbd>L</kbd> can be typed into** — the server you want is one word away.

**Panes you can pick up**
- **Drag a pane onto another and they swap places.**
- **Drop a file onto a pane** and its path goes to the agent in it.
- **Paste a picture into a pane on a server and get a link back.** <kbd>⌘</kbd><kbd>V</kbd> on a remote pane now uploads the image and inserts the link.

**Files and the pointer**
- **<kbd>⌘</kbd><kbd>E</kbd> answers the pointer properly:** a hand over a file, a caret in the search box, an arrow everywhere else.
- Search boxes can be clicked into and have a caret of their own.

**Also**
- Copy button beside a pane's folder path.
- Link hovering says <kbd>⌘</kbd>-click before you know the gesture, and links glow in the pane's own colour.
- A connection test leads with a ring and keeps its log a click away.

---

## 2.38.0

<sub>August 2026</sub>

**The largest release since 2.0 — Markdown Viewer, accent colours, rebuilt palettes, and Shepherd cross-fades**

**Markdown, in Dax and in Finder**
- **Dax renders Markdown itself now.** Its own parser and layout — tables, nested lists, task lists, block quotes, footnotes and fenced code all come out the way the document says.
- **Markdown Viewer** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>R</kbd>): open, edit and save `.md` files with live rendering. Press Space on a Markdown file in Finder and a Quick Look extension shows the same thing.
- **An agent's report is rendered as Markdown**, not printed with asterisks and backticks showing.

**The accent colour is yours**
- Settings › Appearance picks it: Dax's own orange by default, Match macOS, the eight macOS colours, Graphite, or a colour well for anything else.

**The launcher and its palettes, rebuilt**
- One set of shared parts underneath all palettes — the same titled header, field, and rows across Snippets, Agent Watch, Vibe History, Secrets, Ask, Remove BG and File Drop.
- Context Fill is now **Context Form Fill**.

**Shepherd**
- **⌘L opens a machines card**, **⌘N makes a project**, **⌘O opens one**.
- **Switching machines cross-fades**; the herd you leave is parked rather than demolished.
- **Connections survive** across switches, drops and sleep.
- An empty pane is the button; killing a pane is quick; a killed agent gets a skull.

**Usage and credits**
- **One-time top-ups replace bundle subscriptions.** Usage screen simplified to the balance and the packs.

**What has been taken out**
- **Collab (Share/Join)** removed — being replanned from scratch. Connecting to a server is untouched.
- **PRD Maker, Clipboard History, Command History and Suggest a Tool** shelved — off the launcher, not deleted.

---

## 2.37.1

<sub>August 2026</sub>

**Shepherd keyboard fix**
- **Typing and dictation land in the pane you're looking at.** Keyboard focus stays in the active agent, so keystrokes, Willow, Wispr Flow and macOS Dictation all reach the right terminal.

---

## 2.37.0

<sub>August 2026</sub>

**Dax Shepherd & Agent Monitor**
- **The herd tool is called Dax Shepherd**, and it has a place on the Home screen.
- **Agent Monitor** sorts agents by what they want from you: Needs you, Working, Done, Idle. View as a List or as a Flock.
- **New Agent** knows what the machine on screen has installed and offers to install what it hasn't — all nineteen agents in one press.
- Keys: ⌘B sidebar, ⌘F full screen, ⌘/ the whole list.
- Dax draws in its own orange on every Mac, whatever the system accent is set to.

---

## 2.35.0

<sub>August 2026</sub>

**Live Agent Tracking & Setup Overhaul**
- **Herdr tracks what every agent is doing as it happens** — an agent that finishes or stops to ask you something changes on the spot.
- **A machine with no coding agent installed offers you one** (server or this Mac).
- **Setting Dax up asks for one permission at a time**, and asks once.
- **Image Ask goes straight from the snip to the question** — annotate with ⌘E when it helps.
- **The .pkg installer works on a Mac set to any language.**

---

## 2.34.0

<sub>August 2026</sub>

**Herdr: adding a server tells you what happened**
- **Test is in the server dialog now**, next to Done. It shows the actual connection log — which address answered, which key was offered, what the server accepted — with a Copy button for the whole thing.
- **Pasted addresses just work.** Text copied from a cloud console often carries invisible characters (non-breaking spaces, zero-width spaces) that ssh refuses. Dax strips them automatically, and a server already saved with one starts working on the next try.
- **Downloaded key files just work.** A `.pem` from AWS arrives readable by everyone on the Mac, which ssh won't touch. Dax narrows it to owner-only for you.
- **When something is genuinely wrong, the message names it** — the field to look at, or the file and the one command that fixes it.

---

## 2.33.1

<sub>August 2026</sub>

**Herdr server fix**
- A key file downloaded from a cloud console now works on the first try. Dax narrows its permissions to owner-only instead of failing silently.

---

## 2.33.0

<sub>August 2026</sub>

**Herdr on a server** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>.</kbd>)
- **Connect a server over SSH and the herd runs there.** Same window, same tabs, same agents, running on the machine with the repo, the RAM and the uptime. Add a host in Settings → Herdr → Servers and pick it from the switcher.
- **Tabs.** <kbd>⌘</kbd><kbd>T</kbd> for a tab, <kbd>⌘</kbd><kbd>N</kbd> for an agent, <kbd>⌃</kbd><kbd>Tab</kbd> to walk the bar, <kbd>⌘</kbd><kbd>W</kbd> to close an agent. Name a tab while you still remember what it was for.
- **Remote Panel.** The server behind the herd, in five tabs: Coding Agents (with one-click update), Services & Ports, Web Servers & SSL, Usage and Utilities.

**New: PRD Maker** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>E</kbd>)
- Say what you want to make and get the brief for it: a composer to start from, three ways to be asked, answers you can go back into, and a document with no holes in it.

**New: Remove BG** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>D</kbd>)
- The subject and nothing else. Snip or pick a picture, look at the cutout, and leave with the image or a link to it. Runs entirely on-device.

**Ask AI**
- **Web search and Deep Research.** A globe for a quick web search, a magnifier for a cited Deep Research answer that reads multiple sources.
- **Markdown rendered.** Headings, lists, code blocks with a copy button.
- **Drag the panel** by the grip. Click any attached image to open it full size.

**File Drop**
- **Shake to drop.** Pick up a file, wiggle it, and a drop shelf appears under the pointer. Drop on it and the upload starts immediately.

**Your first five minutes**
- An interactive demo that walks you to your first badge. Badges arrive as a card with a lap of light and some confetti, and belong to your account, not the Mac.

**Keeping the Mac awake**
- Close the lid and keep working. One switch in Settings, a helper that installs itself.

**Also**
- **Coding agents, found and installed for you.** Settings → Agents shows which CLIs are on this Mac and offers the ones that aren't.
- **Appearance pane** with launcher blur.
- **One Dax at a time** — a second copy hands the window back to the one already running.
- Inject MD can share a file as a link to a remote agent.
- **Fanout has been retired**; saved profiles carry over to Herdr projects.

---

## 2.31.0

<sub>August 2026</sub>

**Clipboard History**
- Links show what they point at (the image, the PDF, the JSON). The list fills in its own thumbnails.
- Code is syntax-coloured (JSON, JavaScript, Swift, Python, Ruby, Go, Rust, and more).
- Grouped by when you copied it: Today, Yesterday, Previous 7 Days.

**Image viewer**
- Scroll to zoom, anchored on the pointer. 10% to 1000%, and pinch works anywhere across the panel.

**Agent Watch**
- What each session has spent, climbing while you watch.
- Stop a session from the list, or <kbd>⌘</kbd><kbd>⌫</kbd> on the selected row.

**Also**
- Annotate has a blur tool (<kbd>R</kbd>).
- Image to Prompt asks which style you want.
- The shortcuts board shows your version.

---

## 2.32.0

<sub>August 2026</sub>

**New: Herdr** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>.</kbd>)
- **Mission control for coding agents.** Connect a folder and Herdr opens a window of live terminals (Claude Code, Codex, Gemini, whichever you use) running side by side in the same project. Add an agent from a menu; you never open a raw shell.
- **Split by dragging.** Drag a pane against the edge of another and the space divides. Even Out Terminals squares up the seams without restarting anything.
- **A tree that tells you what's running.** Groups hold agents, agents show their brand and say when they're working. Rename, close, collapse, reorder.

**New: Clip to Link** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>;</kbd>)
- **Record a region, get a link.** Drag a box, record, and the clip uploads as GIF, MP4, or MOV. The format you used last is the one it offers next.
- **GIFs that move at the speed they were recorded.** Clips have sound (MOV), and MOV shows its size before you send it.

**A home screen**
- **Somewhere to land.** The Dock icon opens a page with your photo, your name, and the three keys worth knowing.
- **A shelf of badges** that fills up as you use Dax. Click one for its page, or the tail of the stack for the whole trophy case.

**Achievements**
- Points, streaks, and badges, earned by using tools. Levels from Newcomer to Legend, milestones like Century and Night Owl, and mastery coins (bronze, silver, gold) per tool.
- Progress syncs with your account so a reinstall or a second Mac doesn't start you over.

**Notifications**
- **Do Not Disturb** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>Z</kbd>): silence every toast without leaving what you're doing.
- **Toasts go to Notification Centre** and can be answered from there.
- **Hovering a toast holds it still.**

**Also**
- Logging out takes the session with it; every signed-in window closes.
- Server faults now say so in a sentence and retry once quietly.
- Snips photograph the screen before the menu can close.
- Annotate has a blur tool and opens behind a scrim that tells you ⌘↩ sends.
- Settings closes on Escape. Palettes have a Back button.

---

## 2.30.0

<sub>August 2026</sub>

**The menu bar shows everything**
- Three columns instead of a scrolling list, with every tool on one screenful. On narrower displays it drops to two columns, or one.
- When signed out, the menu asks you to sign in instead of listing tools that can't run.

**Agent Watch**
- Every session gets its own character. Two agents in one repo are now numbered (Dax 1, Dax 2) on the corner of their avatar and in their name.
- Only what's actually running. Close a terminal and the session leaves within seconds.
- Selecting a session raises the right tab, even when two share a folder.

**Dax Shots**
- Image to URL, Image Ask, Image to Prompt, and Image to Text now save every picture you hand them to `~/Pictures/Dax Shots`, one folder per tool.

**Clipboard History**
- The menu-bar icon briefly becomes a downward arrow when something is copied. The chime is off by default now.

**Signing up**
- The profile photo is its own step (and skippable). The welcome screen closes itself with a short countdown.

**Also**
- Open Project remembers the agent you picked between launches.
- Long toasts from AI Text Transform, Image to Text, and Context Fill no longer clip.
- The version number sits at the foot of Settings and opens the changelog.

---

## 2.29.0

<sub>August 2026</sub>

**New: Agent Watch** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>M</kbd>)
- Every Claude Code session you have running, in one panel. The project, the last thing the agent said, and what it's doing right now.
- A character for every session: twenty-one pixel animals. They walk while working, shuffle when waiting, stand still when done.
- Status lights (green, yellow, grey) and a timer for how long each session has been in its state.
- Answer from the notification: type a reply and Dax puts it in the exact terminal tab that asked.
- Open Project's toast now shows the agent's own logo or a folder icon.

---

## 2.28.0

<sub>August 2026</sub>

**New: Clipboard History** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>C</kbd>)
- Everything you copy (text, images, files), searchable, with a full preview. Source app shown per entry. Password-manager concealed copies are never recorded.

**New: Prompt Snippets** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>J</kbd>)
- The prompts you keep retyping, one shortcut away. Toggle between copying and typing into the focused app.

**Image annotation editor**
- Draw arrows, boxes, and labels on a screenshot before sending it to Image to URL, Image Ask, or Image to Prompt. Toggle per tool in Settings.

---

## 2.27.1

<sub>August 2026</sub>

**Privacy fix for profile photos**
- Photos you upload are now stripped of embedded metadata (location, device, time) before they leave your Mac.

---

## 2.27.0

<sub>August 2026</sub>

**Full screen, right on the image**
- A **Full Screen** button now sits on generated images, fading in on the top-right corner. <kbd>⌘</kbd><kbd>F</kbd> still works too.

**Profile photos that stay current**
- Change your photo on another device and it arrives here. Dax checks for a new photo as it refreshes your session.

---

## 2.26.0

<sub>August 2026</sub>

**Open signup**
- No invite code needed to get started anymore.

**Coming back to Dax**
- One tap to sign back in: your avatar, name, and a **Send me a code** button. No retyping your email.

**Full-screen images** (<kbd>⌘</kbd><kbd>F</kbd>)
- Press <kbd>⌘</kbd><kbd>F</kbd> on a generated image to blow it up full screen. Pinch to zoom, actual-size for a 1:1 pixel look.

**Polish**
- Open Project: press <kbd>⇧</kbd><kbd>⇥</kbd> to cycle through agents in the palette.
- File Drop now handles uploads up to 1 GB.
- Frequent CLIs: `kimi --yolo` ships as a stock entry.

---

## 2.24.0

<sub>July 2026</sub>

**New: Secrets** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>V</kbd>)
- Hand an API key to a coding agent without ever pasting it into the chat. Store the secret in your Keychain, and Dax gives you a `dax run --secret …` command. When the agent needs it, a local broker asks you to approve, never printed, never in the conversation.

**New: Context Fill** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>X</kbd>)
- Filling out a form? Snip the question, get a paste-ready answer from your Context Pack (résumé, docs, links). Snip several and it answers each. No match? It makes a labelled guess. Refine any answer with a follow-up.

**New: Fanout** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>D</kbd>)
- One shortcut, a whole dev setup. Add the repos you work on together, hit <kbd>⌃</kbd><kbd>⌥</kbd><kbd>D</kbd>, and Dax opens a terminal in each with your agent running, tiled side by side.

**Polish**
- Image to URL: press <kbd>⇥</kbd> to switch between copying the link and the image.
- Boilerplates: share a template as a single `.daxel` file. Double-click to install.
- Generate Image: press <kbd>⌘</kbd><kbd>⇧</kbd><kbd>S</kbd> to snip a screen region into the prompt.
- Notch-friendly: new **Show icon in the Dock** option for Macs with a notch.

---

## 2.23.0

<sub>July 2026</sub>

A new local tool for your shell history, plus stickier answer dialogs.

**New: Command History** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>Y</kbd>)
- **Every command you've run, fuzzy-searchable.** Reads your shell history (zsh, bash, fish, sh, ksh, tcsh), 100% local and read-only. Nothing ever leaves your Mac. The commands you use most lead before you type a thing.
- **Pick what <kbd>↩</kbd> does, live.** A toggle in the palette (or <kbd>⌘</kbd><kbd>T</kbd>) switches between **Insert** into the focused terminal, open in a **New Terminal**, or **Copy**, remembered between sessions. <kbd>⌘</kbd><kbd>↩</kbd> inserts and runs; <kbd>⌘</kbd><kbd>C</kbd> always copies.
- **Tune what shows.** A "Start with" prefix seeds the search bar (backspace it to widen), an exclude list hides throwaway commands (ls, cd, clear...), and a filter regex keeps only real *command + arguments* lines by default. Near-duplicates are merged.
- **Safe by default.** Destructive-looking commands (rm -rf, sudo, dd...) are inserted for review but never auto-run.

**Stickier answers**
- **Ask AI & Image Ask stay open** until you close them (<kbd>esc</kbd> or the close button). No more losing an answer to a stray click.

**Polish**
- Signed, notarized, universal (Apple Silicon & Intel), macOS 14+.

---

## 2.1.0

<sub>2026</sub>

Four new tools for the agent-coding workflow. The suite is now ten strong.

- **Vibe History** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>S</kbd>): your last 20 Claude Code & Codex conversations, grouped by date and project. Pick one and the resume command is typed straight into your terminal.
- **Image Ask** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>I</kbd>): draw a box on the screen and ask a question about it; keep asking follow-ups about the same capture.
- **Open Project** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>O</kbd>): reopen any project you've run an agent in, with your terminal or editor of choice (Terminal, iTerm, Ghostty, WezTerm, kitty, VS Code, Cursor, Windsurf, Xcode...). Optionally pick the command per launch.
- **Inject MD** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>A</kbd>): drop AGENTS.md, CLAUDE.md, BOOTSTRAP.md, SOUL.md, BACKLOG.md (or your own files) into any project folder, templates included.

**Better asking**
- **Follow-ups in Ask AI & Image Ask:** answers build into a conversation; the field keeps focus, <kbd>↩</kbd> asks the next question with context, <kbd>⌘</kbd><kbd>C</kbd> copies the latest answer, <kbd>⌘</kbd><kbd>N</kbd> starts over.

**Launcher**
- **Search bar:** <kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>Space</kbd>, type to filter, and every cell is numbered: press <kbd>1</kbd>–<kbd>9</kbd> to run it. Pages of nine with an indicator when the suite outgrows one grid.

**Polish**
- Screen captures can no longer include the selection overlay or cursor pill, now excluded at the capture layer.
- Clearer onboarding (a real keycap instead of an ambiguous glyph), centered empty states, sharper project icons with tech badges.
- Signed, notarized, universal (Apple Silicon & Intel), macOS 14+.

---

## 2.0.0

<sub>2026</sub>

The toolbox grew up. VibeTools for Mac is now a suite of six tools, each a keystroke away.

- **Six tools, one launcher:** press <kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>Space</kbd> to see every tool and its shortcut in one grid.
- **Image to URL** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>U</kbd>): clipboard image to a shareable link.
- **Image to Prompt** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>P</kbd>): reverse-engineer a copied image into a prompt that recreates it.
- **Quick Question** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>K</kbd>): a floating panel for a short answer, in place.
- **File Drop** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>L</kbd>): drop files or folders; one link lands on your clipboard.
- **AI Text Transform** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>\</kbd>): select text anywhere and rewrite it: improve, enhance a prompt, shorten, fix grammar, or a custom instruction.
- **Frequent CLIs** (<kbd>⌃</kbd> <kbd>⌥</kbd> <kbd>↩</kbd>): still here, still entirely on-device.

**Polish**
- **Refreshed setup assistant:** an animated intro, a clear step-by-step progress bar, calmer transitions, and permissions that update live with an obvious "reopen the app" prompt for Screen Recording.
- **New dark-glass look** throughout, with the red-orange accent applied consistently (including links).
- **Full keyboard navigation** in every tool, morphing toasts with a countdown, and a post-setup tip that points you at the launcher.
- Signed, notarized, universal (Apple Silicon & Intel), macOS 14+.

---

## 1.0.1

<sub>2026</sub>

- **New app icon.** VibeToolsForMac finally looks like itself in Finder and dialogs.
- **Intel Macs supported.** The app is now a universal binary.
- **Runs on macOS 14 Sonoma and later** (previously required macOS 26).
- **Update window always comes to the front** when a new version is found.
- **Launch at Login** can now be enabled right from the welcome wizard.
- Hardened runtime enabled; smoother command saving while editing.
